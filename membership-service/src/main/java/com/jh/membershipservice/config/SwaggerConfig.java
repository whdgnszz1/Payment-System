package com.jh.membershipservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // SpringDoc OpenAPI는 자동 설정을 사용합니다.
    // application.yml에서 설정을 관리합니다.
}